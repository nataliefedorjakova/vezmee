import { colors } from '@/styles/colors';
import { fonts } from '@/styles/fonts';

export const theme = {
	light: {
		colors: colors.light,
		fonts,
	},
	dark: {
		colors: colors.dark,
		fonts,
	},
};
